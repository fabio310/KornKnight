<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
	<OutputType>Exe</OutputType>
	<TargetFramework>net8.0</TargetFramework>
	<Nullable>enable</Nullable>
	<ImplicitUsings>enable</ImplicitUsings>
	<AssemblyName>ChessBot.EloEvaluator</AssemblyName>
	<RootNamespace>ChessBot.EloEvaluator</RootNamespace>
	<LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
	<!-- The sweep mode plays real games; the game loop lives in MatchRunner. -->
	<ProjectReference Include="..\ChessBot.MatchRunner\ChessBot.MatchRunner.csproj" />
  </ItemGroup>

</Project>
